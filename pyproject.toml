[project]
name = "politician-dashboard"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pdfplumber>=0.11.10",
    "psycopg[binary]>=3.3.4",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
